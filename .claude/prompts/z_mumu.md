**Initial prompt** 
The goal is to do a full measurement of the Z(mumu) cross section using CMS open data. The analysis will include the data-driven estimation of the fakes and MCs for the signal and the z(ee), dibosons, there is the whole list. Don't use the QCD MC sample, this will be data-driven. There is a skimmed version of the SingleMuon data available in /dcache/atlas/kdevries/BND2026/DoubleMuonSkimmed. A first attempt of performing the cross section measurement is already done and all the results and documentation is available in /project/atlas/users/kdevries/BND2026/BND-school/z-mumu. Then the idea is to fit the invariant mass using the binned likelihood method to extract the cross-section. The ultimate end goal is to have the same analysis for the Z(tautau) and Z(ee) with the aim of doing a combination to extract the cross section using all the histograms from the individual decay channels with a combined fit. For the Z(mumu) analysis we also need to extract the QCD background, aka fakes. To do this use the old time normal Fake Factor. There are MC samples available, this was not used in the first iteration. Before doing anything pull the repository. The data is stored in /dcache/atlas/sjankovy/BND. Make sure to properly document the steps and write CLAUDE.md files and README.md files, so that the other agents running the combinations and other channels could use both the physics knowledge and the code running. In case of doubt study the public CMS and ATLAS results. Produce relevant plots in the runtime, like the MC - data comparisons etc. Also produce a skimmed version of the datasets, so that people can use these on the laptops. 
