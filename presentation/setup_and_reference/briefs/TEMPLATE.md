# Chapter brief — <chapter> (<section folder>)

Status: draft | approved (YYYY-MM-DD, <role of who approved>)
Written by `channel-animation-director` from the interview; the contract for
`channel-story-builder`, `plot-recreator` and the reviewers. Anchors: `../06-chapter-anchors.md`.

## 1. Scope
- Section folder / number: `<S>_<section>` (<S>)
- Time budget: <≈ min> → <n> clips of ≈ <s> s
- Audience: <…>
- **The one sentence the audience remembers**: <…>

## 2. Story
- Spine nodes this chapter spends time on: <…>; skipped on purpose: <…>
- The method explained in depth: <…> (source: `<doc path>`)
- Left out on purpose: <…>
- Tone: real data / schematic / schematic mechanism then real result

## 3. Numbers on screen
Result version shown: <tag / version / date> (source: `<handoff or results file>`).

| symbol | value as printed | source (file § or line) | frozen in (`data/<chapter>_*.json` key) |
|--------|------------------|-------------------------|-------------------------------------------|
| | | | |

Format: <rounding, symmetric / asymmetric, stat/syst/lumi split>

## 4. Entry and exit
- Opens on: <final frame of `<S>-<NN>_<clip>` (scene `<file>` `<Class>`) | fresh | spine strip>
- Event display: <real event (run/event) | typical real event | schematic>
- Ends on: <result point beside the THEORY line on axis <range> | plot | …>

## 5. Clips

| letter | clip name | the one idea | what moves | numbers used | source |
|--------|-----------|--------------|-----------|--------------|--------|
| a | `<chapter>_a_<idea>` | | | | |

## 6. Look
- Layout: μμ defaults (06 §B3) | own: <anchor changes>
- Motifs reused (06 §B4): <…>
- Colour ideas used (06 §C1): <…>
- Palette requests (need the user's OK, added to `style/palette.py` add-only): <sample → proposed tint>
- Pace: calm | brisk; things the team liked / disliked in the existing previews: <…>

## 7. Deviations from the defaults (06 §B), with the reason
- <…>

## 8. Process
- Critique by: <role>; reviews at delivery: physics / style / continuity (yes/no)
- Scene file: `scenes/s<S>_<chapter>_story.py`
- Git: commit locally when approved | leave uncommitted; push: only when asked

## 9. Open questions
- <…>
