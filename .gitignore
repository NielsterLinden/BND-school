# data files – never commit these
*.root
*.parquet
*.h5
*.hdf5
*.npy
*.npz
data/
*.csv.gz

# python / jupyter
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
env/
*.egg-info/

# editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# TRExFitter: the source is the git submodule TRExFitter-v1.8.0/ (build/ is ignored inside it)
TRExFitter-v1.8.0.zip

# fit inputs and outputs (regenerated by each channel's fit scripts)
*/fit/fitinputs/
*/fit/results/
combination/fit/results/

# presentation (Manim): render scratch and delivered videos stay out of git;
# scenes / style / tools / data json / docs are committed
presentation/work/
presentation/media/
presentation/clips/**/*.mp4
presentation/**/*.png
!presentation/figures/*.png

.asetup.save
z-mumu/review/review_pass.pkl
z-mumu/review/deck/build.log

# presentation build scratch
presentation/*.log
presentation/*.aux
presentation/*.nav
presentation/*.out
presentation/*.snm
presentation/*.toc
presentation/*.vrb
