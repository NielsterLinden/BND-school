**Initial prompt** 
The goal is to do a full measurement of the Z(tautau) fully hadronic cross section using CMS open data. The analysis will include the data-driven estimation of the fakes and MCs for the signal and the z(ee), dibosons, there is the whole list. Don't use the QCD MC sample, this will be data-driven. So first you need to do some skimming of the dataset and do proper selection. Afterwards we need to do some MET correction to the visible ditau mass using either the collinear approximation or better the MMC. Then the idea is to fit the invariant mass using the binned likelihood method to extract the cross-section. The ultimate end goal is to have the same analysis for the Z(mumu) and Z(ee) with the aim of doing a combination to extract the cross section using all the histograms from the individual decay channels with a combined fit. For the Z(tautau) analysis we also need to extract the QCD background, aka fakes. To do this use the old time normal Fake Factor. Before doing anything pull the repository and make sure it's up to date. The data is stored in /dcache/atlas/sjankovy/BND. Make sure to properly document the steps and write CLAUDE.md files and README.md files, so that the other agents running the combinations and other channels could use both the physics knowledge and the code running. Make use of the existing CLAUDE/README.md files and also the general description of the frameworks to be used in prompts/general.md. In case of doubt study the public CMS and ATLAS results. Produce relevant plots in the runtime, like the MC - data comparisons etc. Also produce a skimmed version of the datasets, so that people can use these on the laptops.  Make sure to include relevent plots that would justify your methods and results. Include all the relevant uncertainties in the fit and also derive the FF uncertainty as well.
